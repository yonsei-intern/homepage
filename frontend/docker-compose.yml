services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5176:5176"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 5176
    stdin_open: true
    tty: true
